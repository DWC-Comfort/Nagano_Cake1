<div class = "container">
  <p><%= flash[:notice]%></p>
  <div class = "row mt-5">
    <h2>商品詳細</h2>
  </div>

  <div class = "row mt-5">
    <div class = "col-md-4">
      <%= attachment_image_tag @item, :image, :fill, 300, 300, fallback:"no_image.jpeg", size: "300x300"  %>
    </div>

    <div class = "col-md-8">
      <table class = "table">
        <tr>
          <th>商品名</th>
          <td><%= @item.name %></td>
        </tr>

        <tr>
          <th>商品説明</th>
          <td><%= @item.introduction %></td>
        </tr>

        <tr>
          <th>ジャンル</th>
          <td><%= @item.genre.name %></td>
        </tr>

        <tr>
          <th>
            税込価格</br>(税抜価格)
          </th>
          <td>
            <%= item.add_tax_price.to_s(:delimited, delimiter: ',') %>(<%= @item.price %>)円
          </td>
        </tr>

        <tr>
          <th>販売ステータス</th>
          <td>
            <% if @item.is_active == false %>
            <span>販売停止中</span>
            <% else %>
            <span>販売中</span>
            <% end %>
          </td>
        </tr>
      </table>
      <%= link_to "編集する",edit_admin_item_path(@item.id) %>
    </div>
  </div>
</div>