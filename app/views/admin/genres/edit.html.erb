<p>ジャンル編集<p>

<!-- ジャンル編集機能ここから -->
　<%= form_with model: ([:admin, @genre]), local: true do |f| %>
　　<%= f.label :ジャンル %>
　　<%= f.text_field :name, autofocus: true, placeholder: "ジャンル名" %>
　　<%= f.submit "変更を保存" %>
　<% end %>
<!-- ジャンル編集機能ここまで -->